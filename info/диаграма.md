classDiagram
class UserService {
+getUser(id: String): User
+createUser(user: User): User
+updateUser(id: String, user: User): User
+deleteUser(id: String): void
}

class OrderService {
+getOrder(id: String): Order
+createOrder(order: Order): Order
+cancelOrder(id: String): boolean
+calculateTotal(order: Order): double
}

class PaymentService {
+processPayment(payment: Payment): PaymentResult
+refundPayment(paymentId: String): boolean
+validatePayment(payment: Payment): boolean
}

class NotificationService {
+sendEmail(to: String, subject: String, body: String): boolean
+sendSMS(to: String, message: String): boolean
+notifyUser(userId: String, message: String): void
}

UserService ..> OrderService : использует
OrderService ..> PaymentService : использует
OrderService ..> NotificationService : использует
UserService ..> NotificationService : использует